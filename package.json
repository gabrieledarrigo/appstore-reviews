{
  "name": "appstore-reviews",
  "version": "0.0.1",
  "description": "Recent iOS App store reviews viewer",
  "private": true,
  "workspaces": [
    "server",
    "application"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace server\" \"npm run dev --workspace application\"",
    "build": "npm run build --workspace server && npm run build --workspace application",
    "start": "npm run start --workspace server"
  },
  "keywords": [
    "appstore",
    "reviews"
  ],
  "author": "Gabriele D'Arrigo, darrigo.g@gmail.com",
  "license": "UNLICENSED",
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
